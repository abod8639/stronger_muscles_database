# 🎯 Prompt تفصيلي لإنشاء لوحة تحكم متكاملة لمتجر Stronger Muscles

## 📌 نظرة عامة على المشروع

نحن بحاجة إلى بناء **لوحة تحكم إدارية شاملة** لمتجر إلكتروني متخصص في بيع مكملات بناء العضلات والمكملات الغذائية. المتجر يحتوي على هيكل بيانات متكامل يشمل:
- المستخدمين والعملاء
- التصنيفات والمنتجات
- سلة التسوق والطلبات
- نظام الدفع والشحن

---

## 📊 Models وهياكل البيانات

### **1. User Model** - نموذج المستخدم
```php
<?php

namespace App\Models;

class User extends Authenticatable
{
    protected $fillable = [
        'name',              // اسم المستخدم الكامل
        'email',             // البريد الإلكتروني (فريد)
        'password',          // كلمة المرور المشفرة
        'photo_url',         // رابط صورة الملف الشخصي
        'phone_number',      // رقم الهاتف
        'default_address_id',// معرف العنوان الافتراضي
        'preferred_language',// اللغة المفضلة (ar/en)
        'notifications_enabled', // تفعيل الإشعارات
        'is_active',         // حالة الحساب (نشط/معطل)
    ];

    protected function casts(): array {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
            'notifications_enabled' => 'boolean',
            'is_active' => 'boolean',
        ];
    }

    // العلاقات
    public function orders() {
        return $this->hasMany(Order::class);
    }

    public function cartItems() {
        return $this->hasMany(CartItem::class);
    }
}
```

**البيانات المتوقعة:**
- المعرف: رقمي تلقائي (BIGINT)
- البريد الإلكتروني: فريد ومتحقق
- الحالة: نشط/معطل (boolean)
- عدد الطلبات: محسوب من Orders
- إجمالي المصروف: محسوب من Orders

---

### **2. Category Model** - نموذج التصنيف
```php
<?php

namespace App\Models;

class Category extends Model
{
    public $incrementing = false;
    protected $keyType = 'string';

    protected $fillable = [
        'id',                // معرف التصنيف (UUID/Slug)
        'name',              // اسم التصنيف
        'description',       // وصف التصنيف
        'image_url',         // صورة التصنيف
        'sort_order',        // ترتيب العرض
        'is_active',         // حالة التصنيف
    ];

    protected function casts(): array {
        return [
            'sort_order' => 'integer',
            'is_active' => 'boolean',
        ];
    }

    // العلاقات
    public function products() {
        return $this->hasMany(Product::class);
    }
}
```

**البيانات المتوقعة:**
- المعرف: نصي (UUID أو Slug مثل: "protein", "bcaa")
- الصورة: رابط صورة التصنيف
- الترتيب: رقم للترتيب
- عدد المنتجات: محسوب من Products
- الحالة: نشط/معطل

---

### **3. Product Model** - نموذج المنتج
```php
<?php

namespace App\Models;

class Product extends Model
{
    public $incrementing = false;
    protected $keyType = 'string';

    protected $fillable = [
        'id',                      // معرف المنتج (UUID)
        'name',                    // اسم المنتج
        'price',                   // السعر الأصلي
        'discount_price',          // السعر بعد الخصم
        'image_urls',              // روابط الصور (JSON)
        'description',             // الوصف التفصيلي
        'category_id',             // معرف التصنيف
        'stock_quantity',          // الكمية المتاحة
        'average_rating',          // متوسط التقييم (0-5)
        'review_count',            // عدد التقييمات
        'brand',                   // العلامة التجارية
        'serving_size',            // حجم الحصة (مثل: 30g)
        'servings_per_container',  // عدد الحصص بالعبوة
        'is_active',               // حالة المنتج
    ];

    protected function casts(): array {
        return [
            'price' => 'decimal:2',
            'discount_price' => 'decimal:2',
            'stock_quantity' => 'integer',
            'average_rating' => 'decimal:2',
            'review_count' => 'integer',
            'servings_per_container' => 'integer',
            'is_active' => 'boolean',
            'image_urls' => 'array',
        ];
    }

    // العلاقات
    public function category() {
        return $this->belongsTo(Category::class);
    }

    public function cartItems() {
        return $this->hasMany(CartItem::class);
    }

    public function orderItems() {
        return $this->hasMany(OrderItem::class);
    }

    // الخصائص المحسوبة
    public function getDiscountPercentageAttribute() {
        if ($this->discount_price && $this->price > 0) {
            return round(((($this->price - $this->discount_price) / $this->price) * 100), 2);
        }
        return 0;
    }
}
```

**البيانات المتوقعة:**
- المعرف: نصي (UUID مثل: "whey-gold-5lb")
- الأسعار: عددية عشرية (مثل: 250.00, 225.00)
- الصور: مصفوفة JSON (مثل: ["url1.jpg", "url2.jpg"])
- المخزون: رقمي (كمية متاحة)
- التقييم: عشري من 0 إلى 5 (مثل: 4.85)
- الحالة: نشط/معطل

---

### **4. CartItem Model** - نموذج عنصر السلة
```php
<?php

namespace App\Models;

class CartItem extends Model
{
    public $incrementing = false;
    protected $keyType = 'string';

    protected $fillable = [
        'id',                 // معرف العنصر (UUID)
        'user_id',            // معرف المستخدم
        'product_id',         // معرف المنتج
        'product_name',       // اسم المنتج (نسخة مخزنة)
        'price',              // السعر وقت الإضافة
        'image_urls',         // صور المنتج
        'quantity',           // الكمية المطلوبة
        'added_at',           // تاريخ الإضافة للسلة
    ];

    protected function casts(): array {
        return [
            'price' => 'decimal:2',
            'quantity' => 'integer',
            'added_at' => 'datetime',
        ];
    }

    // العلاقات
    public function user() {
        return $this->belongsTo(User::class);
    }

    public function product() {
        return $this->belongsTo(Product::class);
    }

    // الخصائص المحسوبة
    public function getSubtotalAttribute() {
        return $this->price * $this->quantity;
    }
}
```

**البيانات المتوقعة:**
- المعرف: نصي (UUID)
- الكمية: رقمي (1، 2، 3، ...)
- السعر: عددية عشرية (السعر وقت الإضافة)
- تاريخ الإضافة: timestamp

---

### **5. Order Model** - نموذج الطلب
```php
<?php

namespace App\Models;

class Order extends Model
{
    public $incrementing = false;
    protected $keyType = 'string';

    protected $fillable = [
        'id',                         // معرف الطلب
        'user_id',                    // معرف المستخدم
        'order_date',                 // تاريخ الطلب
        'status',                     // حالة الطلب
        'payment_status',             // حالة الدفع
        'payment_method',             // طريقة الدفع
        'address_id',                 // معرف العنوان
        'shipping_address_snapshot',  // نسخة من العنوان (JSON)
        'subtotal',                   // المجموع قبل الشحن والخصم
        'shipping_cost',              // تكلفة الشحن
        'discount',                   // قيمة الخصم
        'total_amount',               // الإجمالي النهائي
        'tracking_number',            // رقم التتبع
        'notes',                      // ملاحظات إضافية
    ];

    protected function casts(): array {
        return [
            'order_date' => 'datetime',
            'subtotal' => 'decimal:2',
            'shipping_cost' => 'decimal:2',
            'discount' => 'decimal:2',
            'total_amount' => 'decimal:2',
            'shipping_address_snapshot' => 'array',
        ];
    }

    // العلاقات
    public function user() {
        return $this->belongsTo(User::class);
    }

    public function orderItems() {
        return $this->hasMany(OrderItem::class);
    }

    // حالات الطلب
    public const STATUS_PENDING = 'pending';      // قيد الانتظار
    public const STATUS_PROCESSING = 'processing'; // قيد المعالجة
    public const STATUS_SHIPPED = 'shipped';       // تم الشحن
    public const STATUS_DELIVERED = 'delivered';   // تم التوصيل
    public const STATUS_CANCELLED = 'cancelled';   // ملغى

    // حالات الدفع
    public const PAYMENT_PENDING = 'pending';      // قيد الانتظار
    public const PAYMENT_PAID = 'paid';            // مدفوع
    public const PAYMENT_FAILED = 'failed';        // فشل
    public const PAYMENT_REFUNDED = 'refunded';    // مسترد
}
```

**البيانات المتوقعة:**
- المعرف: نصي (مثل: "ORD-2025-001")
- التاريخ: timestamp
- الحالة: نص (pending, processing, shipped, delivered, cancelled)
- حالة الدفع: نص (pending, paid, failed, refunded)
- الأسعار: عددية عشرية
- رقم التتبع: نصي أو null

---

### **6. OrderItem Model** - نموذج عنصر الطلب
```php
<?php

namespace App\Models;

class OrderItem extends Model
{
    public $incrementing = false;
    protected $keyType = 'string';

    protected $fillable = [
        'id',              // معرف العنصر (UUID)
        'order_id',        // معرف الطلب
        'product_id',      // معرف المنتج
        'product_name',    // اسم المنتج (نسخة مخزنة)
        'unit_price',      // سعر الوحدة وقت الطلب
        'quantity',        // الكمية
        'subtotal',        // المجموع الفرعي
        'image_url',       // صورة المنتج
    ];

    protected function casts(): array {
        return [
            'unit_price' => 'decimal:2',
            'quantity' => 'integer',
            'subtotal' => 'decimal:2',
        ];
    }

    // العلاقات
    public function order() {
        return $this->belongsTo(Order::class);
    }

    public function product() {
        return $this->belongsTo(Product::class);
    }
}
```

**البيانات المتوقعة:**
- المعرف: نصي (UUID)
- السعر: عددية عشرية (السعر وقت الطلب وليس السعر الحالي)
- الكمية: رقمي
- المجموع: عددية عشرية (محسوبة = unit_price × quantity)

---

## 📋 ملخص العلاقات (Entity Relationships)

```
┌──────────────────────────────────────────────────────────────┐
│                    Users (المستخدمين)                        │
│  id | name | email | phone | is_active | notifications      │
└──────┬──────────────────────────────────────────────────────┘
       │
       ├─── hasMany ──→ Orders (الطلبات)
       │
       └─── hasMany ──→ CartItems (سلة التسوق)

┌──────────────────────────────────────────────────────────────┐
│                  Categories (التصنيفات)                      │
│  id | name | image_url | sort_order | is_active             │
└──────┬──────────────────────────────────────────────────────┘
       │
       └─── hasMany ──→ Products (المنتجات)

┌──────────────────────────────────────────────────────────────┐
│                   Products (المنتجات)                        │
│  id | name | price | stock | rating | category_id           │
└──────┬──────────────────────────────────────────────────────┘
       │
       ├─── hasMany ──→ CartItems
       │
       └─── hasMany ──→ OrderItems

┌──────────────────────────────────────────────────────────────┐
│                    Orders (الطلبات)                          │
│  id | user_id | status | payment_status | total_amount       │
└──────┬──────────────────────────────────────────────────────┘
       │
       └─── hasMany ──→ OrderItems

┌──────────────────────────────────────────────────────────────┐
│                 OrderItems (عناصر الطلب)                     │
│  id | order_id | product_id | unit_price | quantity          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                   CartItems (سلة التسوق)                     │
│  id | user_id | product_id | quantity | price               │
└──────────────────────────────────────────────────────────────┘
```

---

## 💾 أمثلة على البيانات الفعلية

### **مثال على User (المستخدم)**
```json
{
  "id": 1,
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "phone_number": "+966501234567",
  "is_active": true,
  "notifications_enabled": true,
  "preferred_language": "ar",
  "created_at": "2025-12-15T10:30:00Z",
  "updated_at": "2025-12-31T15:45:00Z",
  "orders_count": 5,
  "total_spent": 1250.75
}
```

### **مثال على Category (التصنيف)**
```json
{
  "id": "protein",
  "name": "بروتين",
  "description": "مكملات البروتين لبناء العضلات",
  "image_url": "https://example.com/categories/protein.jpg",
  "sort_order": 1,
  "is_active": true,
  "products_count": 15
}
```

### **مثال على Product (المنتج)**
```json
{
  "id": "whey-gold-5lb",
  "name": "Optimum Nutrition Gold Standard Whey",
  "price": 250.00,
  "discount_price": 225.00,
  "discount_percentage": 10.00,
  "image_urls": [
    "https://example.com/products/whey1.jpg",
    "https://example.com/products/whey2.jpg"
  ],
  "description": "بروتين واي عالي الجودة مع حمض الغلوتامين",
  "category_id": "protein",
  "category_name": "بروتين",
  "stock_quantity": 50,
  "average_rating": 4.85,
  "review_count": 127,
  "brand": "Optimum Nutrition",
  "serving_size": "30g",
  "servings_per_container": 74,
  "is_active": true,
  "in_stock": true,
  "created_at": "2025-12-01T08:00:00Z"
}
```

### **مثال على CartItem (عنصر سلة)**
```json
{
  "id": "cart-item-123",
  "user_id": 1,
  "product_id": "whey-gold-5lb",
  "product_name": "Optimum Nutrition Gold Standard Whey",
  "price": 225.00,
  "quantity": 2,
  "subtotal": 450.00,
  "image_urls": ["https://example.com/products/whey1.jpg"],
  "added_at": "2025-12-31T14:20:00Z"
}
```

### **مثال على Order (الطلب)**
```json
{
  "id": "ORD-2025-001",
  "user_id": 1,
  "user_name": "أحمد محمد",
  "user_email": "ahmed@example.com",
  "order_date": "2025-12-31T10:30:00Z",
  "status": "processing",
  "payment_status": "paid",
  "payment_method": "card",
  "subtotal": 450.00,
  "shipping_cost": 25.00,
  "discount": 0.00,
  "total_amount": 475.00,
  "tracking_number": "TRACK-123456",
  "notes": "يرجى التوصيل في أوقات الصباح",
  "items_count": 2,
  "created_at": "2025-12-31T10:30:00Z",
  "updated_at": "2025-12-31T15:45:00Z"
}
```

### **مثال على OrderItem (عنصر الطلب)**
```json
{
  "id": "order-item-456",
  "order_id": "ORD-2025-001",
  "product_id": "whey-gold-5lb",
  "product_name": "Optimum Nutrition Gold Standard Whey",
  "unit_price": 225.00,
  "quantity": 2,
  "subtotal": 450.00,
  "image_url": "https://example.com/products/whey1.jpg"
}
```

---

## 🏗️ متطلبات لوحة التحكم

### 1️⃣ **لوحة التحكم الرئيسية (Dashboard)**

#### المتطلبات الأساسية:
- **عرض الإحصائيات الرئيسية**:
  - إجمالي الإيرادات (اليوم، هذا الأسبوع، هذا الشهر)
  - عدد الطلبات الجديدة والمعلقة
  - عدد المستخدمين النشطين
  - أفضل المنتجات مبيعاً
  - أفضل التصنيفات
  - معدل التحويل (Conversion Rate)

- **الرسوم البيانية والمخططات**:
  - مخطط بياني خطي لمبيعات آخر 30 يوم
  - مخطط دائري لتوزيع المبيعات حسب التصنيفات
  - مخطط أعمدة لأفضل 5 منتجات
  - مخطط معدل الطلبات (Pending, Processing, Completed, Cancelled)

- **قائمة الأنشطة الأخيرة**:
  - آخر 10 طلبات مُضافة
  - آخر 10 مستخدمين جدد
  - التحديثات الأخيرة على المنتجات

---

### 2️⃣ **إدارة المنتجات (Products Management)**

#### المتطلبات:
- **قائمة المنتجات**:
  - جدول شامل يعرض: الصورة الصغيرة، الاسم، السعر، السعر بعد الخصم، الكمية المتاحة، التصنيف، التقييم، الحالة (نشط/معطل)
  - فلاتر متقدمة:
    - البحث بالاسم أو رقم المنتج
    - التصفية حسب التصنيف
    - التصفية حسب نطاق السعر
    - التصفية حسب حالة الأسهم (متاح/منتهي)
    - عرض المنتجات النشطة فقط أو الكل
  - ترتيب حسب: الاسم، السعر، التاريخ، المبيعات، التقييم
  - Pagination مع خيارات عرض (10، 25، 50 سجل)

- **إضافة منتج جديد**:
  - نموذج شامل يتضمن:
    - اسم المنتج (مع التحقق من التكرار)
    - الوصف (محرر غني Rich Text Editor)
    - السعر والسعر بعد الخصم
    - تحميل الصور (متعدد مع preview)
    - اختيار التصنيف من قائمة منسدلة
    - الكمية المتاحة
    - معلومات الماركة
    - حجم الحصة والحصص في العبوة
    - تفعيل/تعطيل المنتج
    - تحديد الوزن والأبعاد (للشحن)
    - إضافة كود promo/discount خاص

- **تعديل المنتج**:
  - نفس نموذج الإضافة مع قيم مملوءة مسبقاً
  - إمكانية تحديث الصور القديمة أو إضافة جديدة
  - عرض سجل التعديلات (متى وبواسطة من تم التعديل)

- **حذف المنتج**:
  - تأكيد قبل الحذف
  - خيار حذف حقيقي أو "soft delete" (إخفاء فقط)

- **عمليات جماعية (Bulk Operations)**:
  - تحديد عدة منتجات وتنفيذ عمليات عليها:
    - تغيير التصنيف للكل
    - تطبيق خصم على الكل
    - تفعيل/تعطيل الكل
    - حذف الكل

- **استيراد/تصدير**:
  - استيراد منتجات من ملف Excel/CSV
  - تصدير قائمة المنتجات إلى Excel/PDF
  - معاينة قبل الاستيراد

---

### 3️⃣ **إدارة التصنيفات (Categories Management)**

#### المتطلبات:
- **قائمة التصنيفات**:
  - جدول يعرض: الصورة، الاسم، عدد المنتجات، الترتيب، الحالة
  - إمكانية تغيير الترتيب بالسحب والإفلات

- **إضافة/تعديل التصنيف**:
  - اسم التصنيف
  - الوصف
  - صورة التصنيف
  - ترتيب العرض
  - تفعيل/تعطيل

---

### 4️⃣ **إدارة الطلبات (Orders Management)**

#### المتطلبات:
- **قائمة الطلبات**:
  - جدول شامل يعرض: رقم الطلب، العميل، التاريخ، الإجمالي، حالة الطلب، حالة الدفع، الإجراءات
  - فلاتر متقدمة:
    - البحث برقم الطلب أو اسم العميل
    - التصفية حسب حالة الطلب (Pending, Processing, Shipped, Delivered, Cancelled)
    - التصفية حسب حالة الدفع (Pending, Completed, Failed)
    - التصفية حسب نطاق التاريخ
    - التصفية حسب نطاق السعر
  - ترتيب حسب: التاريخ (الأحدث أولاً)، الإجمالي، الحالة

- **تفاصيل الطلب**:
  - معلومات العميل (الاسم، البريد، الهاتف)
  - قائمة المنتجات في الطلب مع الأسعار والكميات
  - تفاصيل الشحن (العنوان، رقم التتبع)
  - تفاصيل الدفع (الطريقة، المبلغ، الوقت)
  - ملاحظات الطلب
  - سجل حالات الطلب (Timeline)

- **تحديث حالة الطلب**:
  - اختيار الحالة الجديدة من قائمة
  - إضافة ملاحظة اختيارية
  - إرسال إشعار للعميل تلقائياً
  - تحديث رقم التتبع (للطلبات المشحونة)

- **إدارة الدفع**:
  - تحديث حالة الدفع يدوياً إن لزم
  - عرض دليل الدفع
  - إصدار فاتورة (Invoice)
  - حساب الأموال المستردة (Refund)

- **العمليات**:
  - طباعة بوليصة الشحن
  - إرسال البريد الإلكتروني للعميل
  - إرجاع الطلب (Return Process)
  - إلغاء الطلب مع استرجاع الأموال

---

### 5️⃣ **إدارة المستخدمين (Users Management)**

#### المتطلبات:
- **قائمة المستخدمين**:
  - جدول يعرض: الصورة، الاسم، البريد، رقم الهاتف، تاريخ الانضمام، عدد الطلبات، الإجمالي المصروف، الحالة
  - فلاتر:
    - البحث بالاسم أو البريد أو الهاتف
    - التصفية حسب تاريخ الانضمام
    - التصفية حسب الحالة (نشط/معطل)
    - عرض المستخدمين بأكثر من طلب واحد (عملاء دائمين)

- **تفاصيل المستخدم**:
  - المعلومات الشخصية
  - عناوين الشحن المحفوظة
  - سجل الطلبات
  - سجل العروض المتاحة
  - معلومات المحفظة/الرصيد

- **تعديل المستخدم**:
  - تحديث البيانات الشخصية
  - تفعيل/تعطيل الحساب
  - إعادة تعيين كلمة المرور
  - منح صلاحيات خاصة (مثل الحصول على عروض خاصة)

- **إدارة الآدرس**:
  - إضافة عناوين شحن جديدة
  - تحديث العناوين
  - حذف العناوين
  - تعيين عنوان افتراضي

---

### 6️⃣ **إدارة الخصومات والعروض (Promotions)**

#### المتطلبات:
- **قائمة أكواد الخصم**:
  - جدول يعرض: الكود، النوع (نسبة/مبلغ ثابت)، القيمة، حد الاستخدام، الاستخدامات الحالية، صلاحية الكود، الحالة

- **إنشاء كود خصم جديد**:
  - اسم الكود
  - نوع الخصم (نسبة مئوية أو مبلغ ثابت)
  - قيمة الخصم
  - حد الاستخدام الكلي
  - حد الاستخدام لكل عميل
  - تاريخ البداية والنهاية
  - تطبيق على منتجات معينة أو كل المتجر
  - حد أدنى للشراء

- **عروض خاصة**:
  - عروض حسب التصنيف
  - عروض حسب المنتج
  - عروض حسب العميل (مثل: خصم للعملاء الجدد)

---

### 7️⃣ **التقارير والتحليلات (Reports & Analytics)**

#### المتطلبات:
- **تقرير المبيعات**:
  - إجمالي المبيعات حسب الفترة الزمنية
  - أفضل المنتجات مبيعاً
  - أفضل التصنيفات
  - معدل النمو
  - العملاء الأكثر إنفاقاً

- **تقرير الأرباح**:
  - الإيرادات الإجمالية
  - تكاليف الشحن
  - قيمة الخصومات المستخدمة
  - صافي الأرباح
  - تحليل هامش الربح

- **تقرير المستخدمين**:
  - عدد المستخدمين الجدد
  - معدل الاحتفاظ (Retention Rate)
  - العملاء النشطين
  - متوسط عدد الطلبات لكل عميل
  - متوسط قيمة الطلب

- **تقرير الجرد**:
  - المنتجات منخفضة الأسهم
  - المنتجات المنتهية
  - المنتجات الزائدة
  - القيمة الإجمالية للمخزون

- **تصدير التقارير**:
  - تصدير إلى Excel/PDF
  - جدولة التقارير الدورية

---

### 8️⃣ **إدارة الإعدادات (Settings)**

#### المتطلبات:
- **الإعدادات العامة**:
  - اسم المتجر والوصف
  - البريد الإلكتروني والهاتف
  - عنوان المتجر
  - معلومات الشركة

- **إعدادات الدفع**:
  - طرق الدفع المفعلة
  - بيانات المحافظ الإلكترونية
  - إعدادات البطاقات الائتمانية

- **إعدادات الشحن**:
  - تكاليف الشحن (ثابتة أو حسب الوزن/المسافة)
  - مناطق الشحن المدعومة
  - المدة المتوقعة للشحن

- **إعدادات البريد الإلكتروني**:
  - قالب البريد الإلكتروني للطلبات
  - البريد الإلكتروني للتنبيهات
  - إعدادات SMTP

- **إعدادات السياسات**:
  - سياسة الإرجاع
  - شروط الخدمة
  - سياسة الخصوصية

---

### 9️⃣ **نظام الحقوق والصلاحيات (Roles & Permissions)**

#### المتطلبات:
- **الأدوار الرئيسية**:
  - **Admin**: صلاحيات كاملة
  - **Manager**: إدارة المنتجات والطلبات والمستخدمين
  - **Sales**: عرض الطلبات وتحديث حالتها فقط
  - **Warehouse**: إدارة المخزون فقط
  - **Support**: الرد على استفسارات العملاء فقط

- **إدارة الأدوار والصلاحيات**:
  - إنشاء أدوار جديدة
  - تعيين الصلاحيات لكل دور
  - تعيين الموظفين للأدوار

---

### 🔟 **سجل الأنشطة والتدقيق (Activity Log & Audit)**

#### المتطلبات:
- عرض سجل الأنشطة:
  - من قام بالتعديل وماذا قام به
  - متى حدث التعديل
  - القيم القديمة والجديدة
  - عنوان IP

---

## 🎨 متطلبات الواجهة

### التصميم والتجربة:
- **Responsive Design**: يعمل بشكل مثالي على الويب والتابلت والموبايل
- **Dark/Light Mode**: دعم المظهر الداكن والفاتح
- **Theme Colors**: استخدام ألوان احترافية وملائمة لمتجر مكملات غذائية
- **Icons**: استخدام رموز واضحة وسهلة الفهم
- **Accessibility**: دعم قارئات الشاشة والملاحة بلوحة المفاتيح

### المكونات:
- **Navigation Sidebar**: قائمة جانبية قابلة للطي مع أيقونات
- **Top Bar**: شريط علوي يعرض المستخدم الحالي والإعدادات السريعة
- **Cards**: بطاقات لعرض الإحصائيات والمعلومات
- **Tables**: جداول قابلة للترتيب والفلترة مع Pagination
- **Forms**: نماذج منظمة مع التحقق من الصحة
- **Modals**: نوافذ منفثقة للعمليات الحساسة
- **Toast/Notifications**: رسائل نجاح وخطأ
- **Loading States**: مؤشرات التحميل أثناء العمليات

---

## 🔧 المتطلبات التقنية

### Frontend:
- **Framework**: Vue.js 3 أو React (حسب تفضيلك)
- **UI Library**: Shadcn/ui أو Ant Design أو Material Design
- **Charts**: Chart.js أو ApexCharts
- **Form Validation**: Vee-validate أو React Hook Form
- **State Management**: Pinia أو Redux
- **HTTP Client**: Axios

### Backend (Laravel):
- **API Resources**: استخدام Laravel API Resources
- **Pagination**: دعم pagination في كل endpoint
- **Filtering & Sorting**: دعم متقدم للفلترة والترتيب
- **Validation**: Form Requests للتحقق من البيانات
- **Authorization**: Gates و Policies للحقوق
- **Caching**: استخدام cache للبيانات الثقيلة
- **Queues**: معالجة العمليات الطويلة بالـ Queue
- **Notifications**: إرسال إشعارات للعملاء

### Database:
- المستخدمة: MySQL/MariaDB
- الهيكل: كما موضح في DATABASE_SCHEMA.md
- العلاقات: بين Users و Orders و Products
- الفهارس: تحسين الأداء للاستعلامات الكبيرة

---

## 📊 الإحصائيات المطلوبة

### الوقت الفعلي:
- عدد الطلبات الجديدة
- الإيرادات الحالية
- عدد المستخدمين النشطين

### يومياً:
- إجمالي المبيعات
- عدد الطلبات
- معدل التحويل

### شهرياً:
- أداء المبيعات
- أفضل المنتجات
- أفضل العملاء
- توقعات المبيعات

---

## 🔐 متطلبات الأمان

- **Authentication**: تسجيل الدخول الآمن مع تشفير كلمات المرور
- **Authorization**: تحقق من الصلاحيات قبل كل عملية
- **HTTPS**: استخدام بروتوكول آمن
- **CSRF Protection**: حماية من هجمات CSRF
- **SQL Injection**: استخدام parameterized queries
- **Rate Limiting**: تحديد معدل الطلبات لمنع الهجمات
- **Audit Log**: تسجيل جميع العمليات الحساسة

---

## 📱 الخطوات التنفيذية

### المرحلة الأولى (المتطلبات الأساسية):
1. ✅ بناء API Endpoints للمنتجات والطلبات والمستخدمين
2. ✅ تصميم لوحة التحكم الرئيسية (Dashboard) الأساسية
3. ✅ إنشاء صفحة إدارة المنتجات
4. ✅ إنشاء صفحة إدارة الطلبات

### المرحلة الثانية (الميزات الإضافية):
5. ✅ نظام الخصومات والعروض
6. ✅ إدارة المستخدمين
7. ✅ التقارير والتحليلات
8. ✅ نظام الحقوق والصلاحيات

### المرحلة الثالثة (التحسينات):
9. ✅ تحسين الأداء والـ Cache
10. ✅ اختبار شامل (Unit & E2E Tests)
11. ✅ التوثيق الكامل للـ API

